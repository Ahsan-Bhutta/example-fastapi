name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Say hello to Ahsan
        run: echo "Hello Ahsan"